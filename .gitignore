# secrets / local config
.env

# runtime state — never commit recipient progress or failures
checkpoint.log
dead-letter.log
*.log

# build output
/emailblast
/bin/
bench/emailblast

# generated test data (regenerate with: go run ./cmd/gencsv)
users_*.csv
bench/*.csv

# OS junk
.DS_Store
